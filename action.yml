name: 'PushJJTRTS3'
description: 'Push jUnit Jacoco test results to S3'
inputs:
  path-tests:
    description: '🛣️'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.path-tests }}
