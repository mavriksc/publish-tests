name: 'PushJJTRTS3'
description: 'Push jUnit Jacoco test results to S3'
inputs:
  who-to-greet:
    description: 'Who to greet'
    required: true
    default: 'World'
runs:
  using: 'docker'
  image: 'Dockerfile'
